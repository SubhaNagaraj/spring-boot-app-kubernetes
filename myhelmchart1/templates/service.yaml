apiVersion: v1
kind: Service
metadata:
  name: {{ include "myhelmchart1.fullname" . }}
  labels:
    {{- include "myhelmchart1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myhelmchart1.selectorLabels" . | nindent 4 }}
